'use strict';

jest.mock('fs');

const reader = require('../../lib/reader-callbacks.js');

describe('File Reader Module', () => {

  it('when given a bad file, returns an error', done => {

  });

});
